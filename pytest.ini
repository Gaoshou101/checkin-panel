[pytest]
# panel/tests covers the panel, tests/ the repo-root desktop shell. Split because the
# container image contains panel/ and not the shell — see tests/conftest.py.
testpaths = panel/tests tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
